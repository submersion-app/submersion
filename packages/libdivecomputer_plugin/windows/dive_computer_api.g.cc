// Autogenerated from Pigeon (v22.7.4), do not edit directly.
// See also: https://pub.dev/packages/pigeon

#undef _HAS_EXCEPTIONS

#include "dive_computer_api.g.h"

#include <flutter/basic_message_channel.h>
#include <flutter/binary_messenger.h>
#include <flutter/encodable_value.h>
#include <flutter/standard_message_codec.h>

#include <map>
#include <optional>
#include <string>

namespace libdivecomputer_plugin {
using flutter::BasicMessageChannel;
using flutter::CustomEncodableValue;
using flutter::EncodableList;
using flutter::EncodableMap;
using flutter::EncodableValue;

FlutterError CreateConnectionError(const std::string channel_name) {
  return FlutterError(
      "channel-error",
      "Unable to establish connection on channel: '" + channel_name + "'.",
      EncodableValue(""));
}

// DeviceDescriptor

DeviceDescriptor::DeviceDescriptor(
  const std::string& vendor,
  const std::string& product,
  int64_t model,
  const EncodableList& transports)
 : vendor_(vendor),
    product_(product),
    model_(model),
    transports_(transports) {}

const std::string& DeviceDescriptor::vendor() const {
  return vendor_;
}

void DeviceDescriptor::set_vendor(std::string_view value_arg) {
  vendor_ = value_arg;
}


const std::string& DeviceDescriptor::product() const {
  return product_;
}

void DeviceDescriptor::set_product(std::string_view value_arg) {
  product_ = value_arg;
}


int64_t DeviceDescriptor::model() const {
  return model_;
}

void DeviceDescriptor::set_model(int64_t value_arg) {
  model_ = value_arg;
}


const EncodableList& DeviceDescriptor::transports() const {
  return transports_;
}

void DeviceDescriptor::set_transports(const EncodableList& value_arg) {
  transports_ = value_arg;
}


EncodableList DeviceDescriptor::ToEncodableList() const {
  EncodableList list;
  list.reserve(4);
  list.push_back(EncodableValue(vendor_));
  list.push_back(EncodableValue(product_));
  list.push_back(EncodableValue(model_));
  list.push_back(EncodableValue(transports_));
  return list;
}

DeviceDescriptor DeviceDescriptor::FromEncodableList(const EncodableList& list) {
  DeviceDescriptor decoded(
    std::get<std::string>(list[0]),
    std::get<std::string>(list[1]),
    std::get<int64_t>(list[2]),
    std::get<EncodableList>(list[3]));
  return decoded;
}

// DiscoveredDevice

DiscoveredDevice::DiscoveredDevice(
  const std::string& vendor,
  const std::string& product,
  int64_t model,
  const std::string& address,
  const TransportType& transport)
 : vendor_(vendor),
    product_(product),
    model_(model),
    address_(address),
    transport_(transport) {}

DiscoveredDevice::DiscoveredDevice(
  const std::string& vendor,
  const std::string& product,
  int64_t model,
  const std::string& address,
  const std::string* name,
  const TransportType& transport)
 : vendor_(vendor),
    product_(product),
    model_(model),
    address_(address),
    name_(name ? std::optional<std::string>(*name) : std::nullopt),
    transport_(transport) {}

const std::string& DiscoveredDevice::vendor() const {
  return vendor_;
}

void DiscoveredDevice::set_vendor(std::string_view value_arg) {
  vendor_ = value_arg;
}


const std::string& DiscoveredDevice::product() const {
  return product_;
}

void DiscoveredDevice::set_product(std::string_view value_arg) {
  product_ = value_arg;
}


int64_t DiscoveredDevice::model() const {
  return model_;
}

void DiscoveredDevice::set_model(int64_t value_arg) {
  model_ = value_arg;
}


const std::string& DiscoveredDevice::address() const {
  return address_;
}

void DiscoveredDevice::set_address(std::string_view value_arg) {
  address_ = value_arg;
}


const std::string* DiscoveredDevice::name() const {
  return name_ ? &(*name_) : nullptr;
}

void DiscoveredDevice::set_name(const std::string_view* value_arg) {
  name_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void DiscoveredDevice::set_name(std::string_view value_arg) {
  name_ = value_arg;
}


const TransportType& DiscoveredDevice::transport() const {
  return transport_;
}

void DiscoveredDevice::set_transport(const TransportType& value_arg) {
  transport_ = value_arg;
}


EncodableList DiscoveredDevice::ToEncodableList() const {
  EncodableList list;
  list.reserve(6);
  list.push_back(EncodableValue(vendor_));
  list.push_back(EncodableValue(product_));
  list.push_back(EncodableValue(model_));
  list.push_back(EncodableValue(address_));
  list.push_back(name_ ? EncodableValue(*name_) : EncodableValue());
  list.push_back(CustomEncodableValue(transport_));
  return list;
}

DiscoveredDevice DiscoveredDevice::FromEncodableList(const EncodableList& list) {
  DiscoveredDevice decoded(
    std::get<std::string>(list[0]),
    std::get<std::string>(list[1]),
    std::get<int64_t>(list[2]),
    std::get<std::string>(list[3]),
    std::any_cast<const TransportType&>(std::get<CustomEncodableValue>(list[5])));
  auto& encodable_name = list[4];
  if (!encodable_name.IsNull()) {
    decoded.set_name(std::get<std::string>(encodable_name));
  }
  return decoded;
}

// ProfileSample

ProfileSample::ProfileSample(
  int64_t time_seconds,
  double depth_meters)
 : time_seconds_(time_seconds),
    depth_meters_(depth_meters) {}

ProfileSample::ProfileSample(
  int64_t time_seconds,
  double depth_meters,
  const double* temperature_celsius,
  const double* pressure_bar,
  const int64_t* tank_index,
  const double* heart_rate)
 : time_seconds_(time_seconds),
    depth_meters_(depth_meters),
    temperature_celsius_(temperature_celsius ? std::optional<double>(*temperature_celsius) : std::nullopt),
    pressure_bar_(pressure_bar ? std::optional<double>(*pressure_bar) : std::nullopt),
    tank_index_(tank_index ? std::optional<int64_t>(*tank_index) : std::nullopt),
    heart_rate_(heart_rate ? std::optional<double>(*heart_rate) : std::nullopt) {}

int64_t ProfileSample::time_seconds() const {
  return time_seconds_;
}

void ProfileSample::set_time_seconds(int64_t value_arg) {
  time_seconds_ = value_arg;
}


double ProfileSample::depth_meters() const {
  return depth_meters_;
}

void ProfileSample::set_depth_meters(double value_arg) {
  depth_meters_ = value_arg;
}


const double* ProfileSample::temperature_celsius() const {
  return temperature_celsius_ ? &(*temperature_celsius_) : nullptr;
}

void ProfileSample::set_temperature_celsius(const double* value_arg) {
  temperature_celsius_ = value_arg ? std::optional<double>(*value_arg) : std::nullopt;
}

void ProfileSample::set_temperature_celsius(double value_arg) {
  temperature_celsius_ = value_arg;
}


const double* ProfileSample::pressure_bar() const {
  return pressure_bar_ ? &(*pressure_bar_) : nullptr;
}

void ProfileSample::set_pressure_bar(const double* value_arg) {
  pressure_bar_ = value_arg ? std::optional<double>(*value_arg) : std::nullopt;
}

void ProfileSample::set_pressure_bar(double value_arg) {
  pressure_bar_ = value_arg;
}


const int64_t* ProfileSample::tank_index() const {
  return tank_index_ ? &(*tank_index_) : nullptr;
}

void ProfileSample::set_tank_index(const int64_t* value_arg) {
  tank_index_ = value_arg ? std::optional<int64_t>(*value_arg) : std::nullopt;
}

void ProfileSample::set_tank_index(int64_t value_arg) {
  tank_index_ = value_arg;
}


const double* ProfileSample::heart_rate() const {
  return heart_rate_ ? &(*heart_rate_) : nullptr;
}

void ProfileSample::set_heart_rate(const double* value_arg) {
  heart_rate_ = value_arg ? std::optional<double>(*value_arg) : std::nullopt;
}

void ProfileSample::set_heart_rate(double value_arg) {
  heart_rate_ = value_arg;
}


EncodableList ProfileSample::ToEncodableList() const {
  EncodableList list;
  list.reserve(6);
  list.push_back(EncodableValue(time_seconds_));
  list.push_back(EncodableValue(depth_meters_));
  list.push_back(temperature_celsius_ ? EncodableValue(*temperature_celsius_) : EncodableValue());
  list.push_back(pressure_bar_ ? EncodableValue(*pressure_bar_) : EncodableValue());
  list.push_back(tank_index_ ? EncodableValue(*tank_index_) : EncodableValue());
  list.push_back(heart_rate_ ? EncodableValue(*heart_rate_) : EncodableValue());
  return list;
}

ProfileSample ProfileSample::FromEncodableList(const EncodableList& list) {
  ProfileSample decoded(
    std::get<int64_t>(list[0]),
    std::get<double>(list[1]));
  auto& encodable_temperature_celsius = list[2];
  if (!encodable_temperature_celsius.IsNull()) {
    decoded.set_temperature_celsius(std::get<double>(encodable_temperature_celsius));
  }
  auto& encodable_pressure_bar = list[3];
  if (!encodable_pressure_bar.IsNull()) {
    decoded.set_pressure_bar(std::get<double>(encodable_pressure_bar));
  }
  auto& encodable_tank_index = list[4];
  if (!encodable_tank_index.IsNull()) {
    decoded.set_tank_index(std::get<int64_t>(encodable_tank_index));
  }
  auto& encodable_heart_rate = list[5];
  if (!encodable_heart_rate.IsNull()) {
    decoded.set_heart_rate(std::get<double>(encodable_heart_rate));
  }
  return decoded;
}

// GasMix

GasMix::GasMix(
  int64_t index,
  double o2_percent,
  double he_percent)
 : index_(index),
    o2_percent_(o2_percent),
    he_percent_(he_percent) {}

int64_t GasMix::index() const {
  return index_;
}

void GasMix::set_index(int64_t value_arg) {
  index_ = value_arg;
}


double GasMix::o2_percent() const {
  return o2_percent_;
}

void GasMix::set_o2_percent(double value_arg) {
  o2_percent_ = value_arg;
}


double GasMix::he_percent() const {
  return he_percent_;
}

void GasMix::set_he_percent(double value_arg) {
  he_percent_ = value_arg;
}


EncodableList GasMix::ToEncodableList() const {
  EncodableList list;
  list.reserve(3);
  list.push_back(EncodableValue(index_));
  list.push_back(EncodableValue(o2_percent_));
  list.push_back(EncodableValue(he_percent_));
  return list;
}

GasMix GasMix::FromEncodableList(const EncodableList& list) {
  GasMix decoded(
    std::get<int64_t>(list[0]),
    std::get<double>(list[1]),
    std::get<double>(list[2]));
  return decoded;
}

// TankInfo

TankInfo::TankInfo(
  int64_t index,
  int64_t gas_mix_index)
 : index_(index),
    gas_mix_index_(gas_mix_index) {}

TankInfo::TankInfo(
  int64_t index,
  int64_t gas_mix_index,
  const double* volume_liters,
  const double* start_pressure_bar,
  const double* end_pressure_bar)
 : index_(index),
    gas_mix_index_(gas_mix_index),
    volume_liters_(volume_liters ? std::optional<double>(*volume_liters) : std::nullopt),
    start_pressure_bar_(start_pressure_bar ? std::optional<double>(*start_pressure_bar) : std::nullopt),
    end_pressure_bar_(end_pressure_bar ? std::optional<double>(*end_pressure_bar) : std::nullopt) {}

int64_t TankInfo::index() const {
  return index_;
}

void TankInfo::set_index(int64_t value_arg) {
  index_ = value_arg;
}


int64_t TankInfo::gas_mix_index() const {
  return gas_mix_index_;
}

void TankInfo::set_gas_mix_index(int64_t value_arg) {
  gas_mix_index_ = value_arg;
}


const double* TankInfo::volume_liters() const {
  return volume_liters_ ? &(*volume_liters_) : nullptr;
}

void TankInfo::set_volume_liters(const double* value_arg) {
  volume_liters_ = value_arg ? std::optional<double>(*value_arg) : std::nullopt;
}

void TankInfo::set_volume_liters(double value_arg) {
  volume_liters_ = value_arg;
}


const double* TankInfo::start_pressure_bar() const {
  return start_pressure_bar_ ? &(*start_pressure_bar_) : nullptr;
}

void TankInfo::set_start_pressure_bar(const double* value_arg) {
  start_pressure_bar_ = value_arg ? std::optional<double>(*value_arg) : std::nullopt;
}

void TankInfo::set_start_pressure_bar(double value_arg) {
  start_pressure_bar_ = value_arg;
}


const double* TankInfo::end_pressure_bar() const {
  return end_pressure_bar_ ? &(*end_pressure_bar_) : nullptr;
}

void TankInfo::set_end_pressure_bar(const double* value_arg) {
  end_pressure_bar_ = value_arg ? std::optional<double>(*value_arg) : std::nullopt;
}

void TankInfo::set_end_pressure_bar(double value_arg) {
  end_pressure_bar_ = value_arg;
}


EncodableList TankInfo::ToEncodableList() const {
  EncodableList list;
  list.reserve(5);
  list.push_back(EncodableValue(index_));
  list.push_back(EncodableValue(gas_mix_index_));
  list.push_back(volume_liters_ ? EncodableValue(*volume_liters_) : EncodableValue());
  list.push_back(start_pressure_bar_ ? EncodableValue(*start_pressure_bar_) : EncodableValue());
  list.push_back(end_pressure_bar_ ? EncodableValue(*end_pressure_bar_) : EncodableValue());
  return list;
}

TankInfo TankInfo::FromEncodableList(const EncodableList& list) {
  TankInfo decoded(
    std::get<int64_t>(list[0]),
    std::get<int64_t>(list[1]));
  auto& encodable_volume_liters = list[2];
  if (!encodable_volume_liters.IsNull()) {
    decoded.set_volume_liters(std::get<double>(encodable_volume_liters));
  }
  auto& encodable_start_pressure_bar = list[3];
  if (!encodable_start_pressure_bar.IsNull()) {
    decoded.set_start_pressure_bar(std::get<double>(encodable_start_pressure_bar));
  }
  auto& encodable_end_pressure_bar = list[4];
  if (!encodable_end_pressure_bar.IsNull()) {
    decoded.set_end_pressure_bar(std::get<double>(encodable_end_pressure_bar));
  }
  return decoded;
}

// DiveEvent

DiveEvent::DiveEvent(
  int64_t time_seconds,
  const std::string& type)
 : time_seconds_(time_seconds),
    type_(type) {}

DiveEvent::DiveEvent(
  int64_t time_seconds,
  const std::string& type,
  const EncodableMap* data)
 : time_seconds_(time_seconds),
    type_(type),
    data_(data ? std::optional<EncodableMap>(*data) : std::nullopt) {}

int64_t DiveEvent::time_seconds() const {
  return time_seconds_;
}

void DiveEvent::set_time_seconds(int64_t value_arg) {
  time_seconds_ = value_arg;
}


const std::string& DiveEvent::type() const {
  return type_;
}

void DiveEvent::set_type(std::string_view value_arg) {
  type_ = value_arg;
}


const EncodableMap* DiveEvent::data() const {
  return data_ ? &(*data_) : nullptr;
}

void DiveEvent::set_data(const EncodableMap* value_arg) {
  data_ = value_arg ? std::optional<EncodableMap>(*value_arg) : std::nullopt;
}

void DiveEvent::set_data(const EncodableMap& value_arg) {
  data_ = value_arg;
}


EncodableList DiveEvent::ToEncodableList() const {
  EncodableList list;
  list.reserve(3);
  list.push_back(EncodableValue(time_seconds_));
  list.push_back(EncodableValue(type_));
  list.push_back(data_ ? EncodableValue(*data_) : EncodableValue());
  return list;
}

DiveEvent DiveEvent::FromEncodableList(const EncodableList& list) {
  DiveEvent decoded(
    std::get<int64_t>(list[0]),
    std::get<std::string>(list[1]));
  auto& encodable_data = list[2];
  if (!encodable_data.IsNull()) {
    decoded.set_data(std::get<EncodableMap>(encodable_data));
  }
  return decoded;
}

// ParsedDive

ParsedDive::ParsedDive(
  const std::string& fingerprint,
  int64_t date_time_epoch,
  double max_depth_meters,
  double avg_depth_meters,
  int64_t duration_seconds,
  const EncodableList& samples,
  const EncodableList& tanks,
  const EncodableList& gas_mixes,
  const EncodableList& events)
 : fingerprint_(fingerprint),
    date_time_epoch_(date_time_epoch),
    max_depth_meters_(max_depth_meters),
    avg_depth_meters_(avg_depth_meters),
    duration_seconds_(duration_seconds),
    samples_(samples),
    tanks_(tanks),
    gas_mixes_(gas_mixes),
    events_(events) {}

ParsedDive::ParsedDive(
  const std::string& fingerprint,
  int64_t date_time_epoch,
  double max_depth_meters,
  double avg_depth_meters,
  int64_t duration_seconds,
  const double* min_temperature_celsius,
  const double* max_temperature_celsius,
  const EncodableList& samples,
  const EncodableList& tanks,
  const EncodableList& gas_mixes,
  const EncodableList& events,
  const std::string* dive_mode)
 : fingerprint_(fingerprint),
    date_time_epoch_(date_time_epoch),
    max_depth_meters_(max_depth_meters),
    avg_depth_meters_(avg_depth_meters),
    duration_seconds_(duration_seconds),
    min_temperature_celsius_(min_temperature_celsius ? std::optional<double>(*min_temperature_celsius) : std::nullopt),
    max_temperature_celsius_(max_temperature_celsius ? std::optional<double>(*max_temperature_celsius) : std::nullopt),
    samples_(samples),
    tanks_(tanks),
    gas_mixes_(gas_mixes),
    events_(events),
    dive_mode_(dive_mode ? std::optional<std::string>(*dive_mode) : std::nullopt) {}

const std::string& ParsedDive::fingerprint() const {
  return fingerprint_;
}

void ParsedDive::set_fingerprint(std::string_view value_arg) {
  fingerprint_ = value_arg;
}


int64_t ParsedDive::date_time_epoch() const {
  return date_time_epoch_;
}

void ParsedDive::set_date_time_epoch(int64_t value_arg) {
  date_time_epoch_ = value_arg;
}


double ParsedDive::max_depth_meters() const {
  return max_depth_meters_;
}

void ParsedDive::set_max_depth_meters(double value_arg) {
  max_depth_meters_ = value_arg;
}


double ParsedDive::avg_depth_meters() const {
  return avg_depth_meters_;
}

void ParsedDive::set_avg_depth_meters(double value_arg) {
  avg_depth_meters_ = value_arg;
}


int64_t ParsedDive::duration_seconds() const {
  return duration_seconds_;
}

void ParsedDive::set_duration_seconds(int64_t value_arg) {
  duration_seconds_ = value_arg;
}


const double* ParsedDive::min_temperature_celsius() const {
  return min_temperature_celsius_ ? &(*min_temperature_celsius_) : nullptr;
}

void ParsedDive::set_min_temperature_celsius(const double* value_arg) {
  min_temperature_celsius_ = value_arg ? std::optional<double>(*value_arg) : std::nullopt;
}

void ParsedDive::set_min_temperature_celsius(double value_arg) {
  min_temperature_celsius_ = value_arg;
}


const double* ParsedDive::max_temperature_celsius() const {
  return max_temperature_celsius_ ? &(*max_temperature_celsius_) : nullptr;
}

void ParsedDive::set_max_temperature_celsius(const double* value_arg) {
  max_temperature_celsius_ = value_arg ? std::optional<double>(*value_arg) : std::nullopt;
}

void ParsedDive::set_max_temperature_celsius(double value_arg) {
  max_temperature_celsius_ = value_arg;
}


const EncodableList& ParsedDive::samples() const {
  return samples_;
}

void ParsedDive::set_samples(const EncodableList& value_arg) {
  samples_ = value_arg;
}


const EncodableList& ParsedDive::tanks() const {
  return tanks_;
}

void ParsedDive::set_tanks(const EncodableList& value_arg) {
  tanks_ = value_arg;
}


const EncodableList& ParsedDive::gas_mixes() const {
  return gas_mixes_;
}

void ParsedDive::set_gas_mixes(const EncodableList& value_arg) {
  gas_mixes_ = value_arg;
}


const EncodableList& ParsedDive::events() const {
  return events_;
}

void ParsedDive::set_events(const EncodableList& value_arg) {
  events_ = value_arg;
}


const std::string* ParsedDive::dive_mode() const {
  return dive_mode_ ? &(*dive_mode_) : nullptr;
}

void ParsedDive::set_dive_mode(const std::string_view* value_arg) {
  dive_mode_ = value_arg ? std::optional<std::string>(*value_arg) : std::nullopt;
}

void ParsedDive::set_dive_mode(std::string_view value_arg) {
  dive_mode_ = value_arg;
}


EncodableList ParsedDive::ToEncodableList() const {
  EncodableList list;
  list.reserve(12);
  list.push_back(EncodableValue(fingerprint_));
  list.push_back(EncodableValue(date_time_epoch_));
  list.push_back(EncodableValue(max_depth_meters_));
  list.push_back(EncodableValue(avg_depth_meters_));
  list.push_back(EncodableValue(duration_seconds_));
  list.push_back(min_temperature_celsius_ ? EncodableValue(*min_temperature_celsius_) : EncodableValue());
  list.push_back(max_temperature_celsius_ ? EncodableValue(*max_temperature_celsius_) : EncodableValue());
  list.push_back(EncodableValue(samples_));
  list.push_back(EncodableValue(tanks_));
  list.push_back(EncodableValue(gas_mixes_));
  list.push_back(EncodableValue(events_));
  list.push_back(dive_mode_ ? EncodableValue(*dive_mode_) : EncodableValue());
  return list;
}

ParsedDive ParsedDive::FromEncodableList(const EncodableList& list) {
  ParsedDive decoded(
    std::get<std::string>(list[0]),
    std::get<int64_t>(list[1]),
    std::get<double>(list[2]),
    std::get<double>(list[3]),
    std::get<int64_t>(list[4]),
    std::get<EncodableList>(list[7]),
    std::get<EncodableList>(list[8]),
    std::get<EncodableList>(list[9]),
    std::get<EncodableList>(list[10]));
  auto& encodable_min_temperature_celsius = list[5];
  if (!encodable_min_temperature_celsius.IsNull()) {
    decoded.set_min_temperature_celsius(std::get<double>(encodable_min_temperature_celsius));
  }
  auto& encodable_max_temperature_celsius = list[6];
  if (!encodable_max_temperature_celsius.IsNull()) {
    decoded.set_max_temperature_celsius(std::get<double>(encodable_max_temperature_celsius));
  }
  auto& encodable_dive_mode = list[11];
  if (!encodable_dive_mode.IsNull()) {
    decoded.set_dive_mode(std::get<std::string>(encodable_dive_mode));
  }
  return decoded;
}

// DownloadProgress

DownloadProgress::DownloadProgress(
  int64_t current,
  int64_t total,
  const std::string& status)
 : current_(current),
    total_(total),
    status_(status) {}

int64_t DownloadProgress::current() const {
  return current_;
}

void DownloadProgress::set_current(int64_t value_arg) {
  current_ = value_arg;
}


int64_t DownloadProgress::total() const {
  return total_;
}

void DownloadProgress::set_total(int64_t value_arg) {
  total_ = value_arg;
}


const std::string& DownloadProgress::status() const {
  return status_;
}

void DownloadProgress::set_status(std::string_view value_arg) {
  status_ = value_arg;
}


EncodableList DownloadProgress::ToEncodableList() const {
  EncodableList list;
  list.reserve(3);
  list.push_back(EncodableValue(current_));
  list.push_back(EncodableValue(total_));
  list.push_back(EncodableValue(status_));
  return list;
}

DownloadProgress DownloadProgress::FromEncodableList(const EncodableList& list) {
  DownloadProgress decoded(
    std::get<int64_t>(list[0]),
    std::get<int64_t>(list[1]),
    std::get<std::string>(list[2]));
  return decoded;
}

// DiveComputerError

DiveComputerError::DiveComputerError(
  const std::string& code,
  const std::string& message)
 : code_(code),
    message_(message) {}

const std::string& DiveComputerError::code() const {
  return code_;
}

void DiveComputerError::set_code(std::string_view value_arg) {
  code_ = value_arg;
}


const std::string& DiveComputerError::message() const {
  return message_;
}

void DiveComputerError::set_message(std::string_view value_arg) {
  message_ = value_arg;
}


EncodableList DiveComputerError::ToEncodableList() const {
  EncodableList list;
  list.reserve(2);
  list.push_back(EncodableValue(code_));
  list.push_back(EncodableValue(message_));
  return list;
}

DiveComputerError DiveComputerError::FromEncodableList(const EncodableList& list) {
  DiveComputerError decoded(
    std::get<std::string>(list[0]),
    std::get<std::string>(list[1]));
  return decoded;
}


PigeonInternalCodecSerializer::PigeonInternalCodecSerializer() {}

EncodableValue PigeonInternalCodecSerializer::ReadValueOfType(
  uint8_t type,
  flutter::ByteStreamReader* stream) const {
  switch (type) {
    case 129: {
        const auto& encodable_enum_arg = ReadValue(stream);
        const int64_t enum_arg_value = encodable_enum_arg.IsNull() ? 0 : encodable_enum_arg.LongValue();
        return encodable_enum_arg.IsNull() ? EncodableValue() : CustomEncodableValue(static_cast<TransportType>(enum_arg_value));
      }
    case 130: {
        return CustomEncodableValue(DeviceDescriptor::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 131: {
        return CustomEncodableValue(DiscoveredDevice::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 132: {
        return CustomEncodableValue(ProfileSample::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 133: {
        return CustomEncodableValue(GasMix::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 134: {
        return CustomEncodableValue(TankInfo::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 135: {
        return CustomEncodableValue(DiveEvent::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 136: {
        return CustomEncodableValue(ParsedDive::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 137: {
        return CustomEncodableValue(DownloadProgress::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    case 138: {
        return CustomEncodableValue(DiveComputerError::FromEncodableList(std::get<EncodableList>(ReadValue(stream))));
      }
    default:
      return flutter::StandardCodecSerializer::ReadValueOfType(type, stream);
    }
}

void PigeonInternalCodecSerializer::WriteValue(
  const EncodableValue& value,
  flutter::ByteStreamWriter* stream) const {
  if (const CustomEncodableValue* custom_value = std::get_if<CustomEncodableValue>(&value)) {
    if (custom_value->type() == typeid(TransportType)) {
      stream->WriteByte(129);
      WriteValue(EncodableValue(static_cast<int>(std::any_cast<TransportType>(*custom_value))), stream);
      return;
    }
    if (custom_value->type() == typeid(DeviceDescriptor)) {
      stream->WriteByte(130);
      WriteValue(EncodableValue(std::any_cast<DeviceDescriptor>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(DiscoveredDevice)) {
      stream->WriteByte(131);
      WriteValue(EncodableValue(std::any_cast<DiscoveredDevice>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(ProfileSample)) {
      stream->WriteByte(132);
      WriteValue(EncodableValue(std::any_cast<ProfileSample>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(GasMix)) {
      stream->WriteByte(133);
      WriteValue(EncodableValue(std::any_cast<GasMix>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(TankInfo)) {
      stream->WriteByte(134);
      WriteValue(EncodableValue(std::any_cast<TankInfo>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(DiveEvent)) {
      stream->WriteByte(135);
      WriteValue(EncodableValue(std::any_cast<DiveEvent>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(ParsedDive)) {
      stream->WriteByte(136);
      WriteValue(EncodableValue(std::any_cast<ParsedDive>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(DownloadProgress)) {
      stream->WriteByte(137);
      WriteValue(EncodableValue(std::any_cast<DownloadProgress>(*custom_value).ToEncodableList()), stream);
      return;
    }
    if (custom_value->type() == typeid(DiveComputerError)) {
      stream->WriteByte(138);
      WriteValue(EncodableValue(std::any_cast<DiveComputerError>(*custom_value).ToEncodableList()), stream);
      return;
    }
  }
  flutter::StandardCodecSerializer::WriteValue(value, stream);
}

/// The codec used by DiveComputerHostApi.
const flutter::StandardMessageCodec& DiveComputerHostApi::GetCodec() {
  return flutter::StandardMessageCodec::GetInstance(&PigeonInternalCodecSerializer::GetInstance());
}

// Sets up an instance of `DiveComputerHostApi` to handle messages through the `binary_messenger`.
void DiveComputerHostApi::SetUp(
  flutter::BinaryMessenger* binary_messenger,
  DiveComputerHostApi* api) {
  DiveComputerHostApi::SetUp(binary_messenger, api, "");
}

void DiveComputerHostApi::SetUp(
  flutter::BinaryMessenger* binary_messenger,
  DiveComputerHostApi* api,
  const std::string& message_channel_suffix) {
  const std::string prepended_suffix = message_channel_suffix.length() > 0 ? std::string(".") + message_channel_suffix : "";
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerHostApi.getDeviceDescriptors" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const flutter::MessageReply<EncodableValue>& reply) {
        try {
          api->GetDeviceDescriptors([reply](ErrorOr<EncodableList>&& output) {
            if (output.has_error()) {
              reply(WrapError(output.error()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerHostApi.startDiscovery" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_transport_arg = args.at(0);
          if (encodable_transport_arg.IsNull()) {
            reply(WrapError("transport_arg unexpectedly null."));
            return;
          }
          const auto& transport_arg = std::any_cast<const TransportType&>(std::get<CustomEncodableValue>(encodable_transport_arg));
          api->StartDiscovery(transport_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerHostApi.stopDiscovery" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const flutter::MessageReply<EncodableValue>& reply) {
        try {
          std::optional<FlutterError> output = api->StopDiscovery();
          if (output.has_value()) {
            reply(WrapError(output.value()));
            return;
          }
          EncodableList wrapped;
          wrapped.push_back(EncodableValue());
          reply(EncodableValue(std::move(wrapped)));
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerHostApi.startDownload" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const flutter::MessageReply<EncodableValue>& reply) {
        try {
          const auto& args = std::get<EncodableList>(message);
          const auto& encodable_device_arg = args.at(0);
          if (encodable_device_arg.IsNull()) {
            reply(WrapError("device_arg unexpectedly null."));
            return;
          }
          const auto& device_arg = std::any_cast<const DiscoveredDevice&>(std::get<CustomEncodableValue>(encodable_device_arg));
          api->StartDownload(device_arg, [reply](std::optional<FlutterError>&& output) {
            if (output.has_value()) {
              reply(WrapError(output.value()));
              return;
            }
            EncodableList wrapped;
            wrapped.push_back(EncodableValue());
            reply(EncodableValue(std::move(wrapped)));
          });
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerHostApi.cancelDownload" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const flutter::MessageReply<EncodableValue>& reply) {
        try {
          std::optional<FlutterError> output = api->CancelDownload();
          if (output.has_value()) {
            reply(WrapError(output.value()));
            return;
          }
          EncodableList wrapped;
          wrapped.push_back(EncodableValue());
          reply(EncodableValue(std::move(wrapped)));
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
  {
    BasicMessageChannel<> channel(binary_messenger, "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerHostApi.getLibdivecomputerVersion" + prepended_suffix, &GetCodec());
    if (api != nullptr) {
      channel.SetMessageHandler([api](const EncodableValue& message, const flutter::MessageReply<EncodableValue>& reply) {
        try {
          ErrorOr<std::string> output = api->GetLibdivecomputerVersion();
          if (output.has_error()) {
            reply(WrapError(output.error()));
            return;
          }
          EncodableList wrapped;
          wrapped.push_back(EncodableValue(std::move(output).TakeValue()));
          reply(EncodableValue(std::move(wrapped)));
        } catch (const std::exception& exception) {
          reply(WrapError(exception.what()));
        }
      });
    } else {
      channel.SetMessageHandler(nullptr);
    }
  }
}

EncodableValue DiveComputerHostApi::WrapError(std::string_view error_message) {
  return EncodableValue(EncodableList{
    EncodableValue(std::string(error_message)),
    EncodableValue("Error"),
    EncodableValue()
  });
}

EncodableValue DiveComputerHostApi::WrapError(const FlutterError& error) {
  return EncodableValue(EncodableList{
    EncodableValue(error.code()),
    EncodableValue(error.message()),
    error.details()
  });
}

// Generated class from Pigeon that represents Flutter messages that can be called from C++.
DiveComputerFlutterApi::DiveComputerFlutterApi(flutter::BinaryMessenger* binary_messenger)
 : binary_messenger_(binary_messenger),
    message_channel_suffix_("") {}

DiveComputerFlutterApi::DiveComputerFlutterApi(
  flutter::BinaryMessenger* binary_messenger,
  const std::string& message_channel_suffix)
 : binary_messenger_(binary_messenger),
    message_channel_suffix_(message_channel_suffix.length() > 0 ? std::string(".") + message_channel_suffix : "") {}

const flutter::StandardMessageCodec& DiveComputerFlutterApi::GetCodec() {
  return flutter::StandardMessageCodec::GetInstance(&PigeonInternalCodecSerializer::GetInstance());
}

void DiveComputerFlutterApi::OnDeviceDiscovered(
  const DiscoveredDevice& device_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerFlutterApi.onDeviceDiscovered" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    CustomEncodableValue(device_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void DiveComputerFlutterApi::OnDiscoveryComplete(
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerFlutterApi.onDiscoveryComplete" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue();
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void DiveComputerFlutterApi::OnDownloadProgress(
  const DownloadProgress& progress_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerFlutterApi.onDownloadProgress" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    CustomEncodableValue(progress_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void DiveComputerFlutterApi::OnDiveDownloaded(
  const ParsedDive& dive_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerFlutterApi.onDiveDownloaded" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    CustomEncodableValue(dive_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void DiveComputerFlutterApi::OnDownloadComplete(
  int64_t total_dives_arg,
  const std::string* serial_number_arg,
  const std::string* firmware_version_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerFlutterApi.onDownloadComplete" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    EncodableValue(total_dives_arg),
    serial_number_arg ? EncodableValue(*serial_number_arg) : EncodableValue(),
    firmware_version_arg ? EncodableValue(*firmware_version_arg) : EncodableValue(),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

void DiveComputerFlutterApi::OnError(
  const DiveComputerError& error_arg,
  std::function<void(void)>&& on_success,
  std::function<void(const FlutterError&)>&& on_error) {
  const std::string channel_name = "dev.flutter.pigeon.libdivecomputer_plugin.DiveComputerFlutterApi.onError" + message_channel_suffix_;
  BasicMessageChannel<> channel(binary_messenger_, channel_name, &GetCodec());
  EncodableValue encoded_api_arguments = EncodableValue(EncodableList{
    CustomEncodableValue(error_arg),
  });
  channel.Send(encoded_api_arguments, [channel_name, on_success = std::move(on_success), on_error = std::move(on_error)](const uint8_t* reply, size_t reply_size) {
    std::unique_ptr<EncodableValue> response = GetCodec().DecodeMessage(reply, reply_size);
    const auto& encodable_return_value = *response;
    const auto* list_return_value = std::get_if<EncodableList>(&encodable_return_value);
    if (list_return_value) {
      if (list_return_value->size() > 1) {
        on_error(FlutterError(std::get<std::string>(list_return_value->at(0)), std::get<std::string>(list_return_value->at(1)), list_return_value->at(2)));
      } else {
        on_success();
      }
    } else {
      on_error(CreateConnectionError(channel_name));
    } 
  });
}

}  // namespace libdivecomputer_plugin
